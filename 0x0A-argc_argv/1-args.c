#include <stdio.h>

/**
 *main - prints the no. of arg
 *@argc: argument count
 *@argv: array of pointers to CLI argument
 *Return: 0
 **/

int main(int argc, char *argv[])
{
printf("argc %d", argc);
return (0);
}
